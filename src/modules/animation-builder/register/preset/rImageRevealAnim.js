import ImageRevealPreset from "@/components/editor/preset/ImageRevealPrest";

AAEAnimBuilder.presets.register({
  groupName: "image",
  presetKey: "wcf-image-reveal-animation",
  name: "Reveal",
  component: ImageRevealPreset,
});
