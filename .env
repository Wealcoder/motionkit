# Absolute path to the motionkit-editor repo root on this machine.
# Used by scripts/copy-to-editor.js to sync built animation scripts into
# the editor server's static folder on every webpack rebuild.
MOTIONKIT_EDITOR_PATH=D:\projects\motionkit-editor
